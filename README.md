# Node RED Scenes Executable

Shell script for https://github.com/denwilliams/node-red-contrib-scenes

Usage:

```
npm install node-red-contrib-scenes-exec -g
node-red-scenes
```

Will default to port `1880`.
